import 'package:flutter/material.dart';

class PaymentPolicyPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child:Text("Payment Policy"),);
  }
}
